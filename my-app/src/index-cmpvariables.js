import React from 'react';
import ReactDOM from 'react-dom';

//variable pattern
const Heading = <h1>Hello React!!</h1> // to be compiled into plain js
ReactDOM.render(Heading, document.getElementById('root'))